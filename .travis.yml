language: go

go:
  - "1.25.x"

install: true

script:
  - go vet ./...
  - go test ./...
  - ./build.sh

deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  skip_cleanup: true
  file_glob: true
  file: "dist/*"
  on:
    tags: true
